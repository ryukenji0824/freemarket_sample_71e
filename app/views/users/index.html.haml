= link_to edit_user_path(@user_id)  do
  ユーザー情報の変更
= link_to edit_address_path(@user_id)  do
  配送先情報の変更
- if @user.card.present?
  = link_to edit_card_path(@user_id)  do
    お支払い情報の変更
- else
  = link_to new_card_path(@user_id)  do
    お支払い情報の登録
